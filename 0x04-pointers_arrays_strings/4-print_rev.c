#include <stdio.h>
#include "holberton.h"

/**
 * print_rev - Prints a string in reverse
 * @s: The string to be reversed
 */

void print_rev(char *str)
{
	int num = 0;

	while (*(str + num) != '\0')
	{
			_putchar('\0');
			num--;
	}
}
